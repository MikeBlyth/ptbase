%fieldset
  %legend Item
  -#= single_line_form(item, item_counter)
  = f.input :drug
  = f.input :dose
  = f.input :units
  = f.input :route
  = f.input :interval, label: "Interval (hours)"
  = f.input :duration, label: "Duration (days)"
